export enum AppRoute {
  Default = '/',
  Catalog = '/catalog',
  Camera = '/catalog/:id',
  Error = '*',
}

export enum APIRoute {
  Cameras = '/cameras',
  Promo = '/promo',
}


export enum NameSpace {
  Data = 'DATA',
}
